apiversion: v1
kind: Service

metadata:
  name: full-stack-1-service

spec:
  type: NodePort

  selector:
    app: full-stack-1

ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30007